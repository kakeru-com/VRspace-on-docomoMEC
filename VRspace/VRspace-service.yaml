apiVersion: v1
kind: Service
metadata:
  name: vrspace-service-xx
spec:
  type: LoadBalancer  # クラウドプロバイダがロードバランサーを自動的に作成
  ports:
  - port: 800
    targetPort: 8080  # コンテナのポート（上記Deploymentで指定したポート）
  selector:
    app: vrspace
