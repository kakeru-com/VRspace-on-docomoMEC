apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: vrspace-service
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: gangrif/vrspace:latest
          env:
            - name: TARGET
              value: "VR Space Knative Service"
